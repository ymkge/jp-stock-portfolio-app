# 国内株式ポートフォリオ管理アプリ (jp-stock-portfolio-app)
<img width="1910" height="960" alt="スクリーンショット 2026-02-06 20 08 09" src="https://github.com/user-attachments/assets/fcc497c9-7c71-44af-aadd-d8c3d27128ee" />
 


## 概要

ユーザーが管理する国内株式、米国株式、投資信託のポートフォリオに基づき、株価や財務指標、配当履歴などをYahoo!ファイナンスから取得し、Webページに一覧表示するシングルページアプリケーション（SPA）です。
各指標が割安か割高かを視覚的に判断するためのサポート機能も搭載しています。

## 主な機能

- **銘柄一覧表示**: 登録された銘柄の各種情報をリアルタイムで取得し、テーブル形式で表示します。
- **決算月表示**: 国内株式・米国株式について、ポートフォリオ一覧に各銘柄の決算月を表示し、権利確定日などの参考情報として活用できます。
- **複数口座対応の保有情報管理**: 1つの銘柄に対して、複数の口座（特定、一般、旧NISA、新NISAなど）での保有情報を個別に管理できます。口座ごとに取得単価と数量を記録し、評価額、損益、税金等を自動計算し、年間配当予想を自動で計算・表示します。
- **ポートフォリオ分析**: 保有銘柄のみを対象とした分析ページで、ポートフォリオ全体のサマリー、保有口座ごとの詳細一覧、業種別・口座種別・**証券会社別**・国別ごとの保有比率を円グラフで視覚的に確認できます。さらに、**ポートフォリオ全体の「DNA（加重平均指標）」や「分散度（HHI指数）」、景気特性やスタイルを判定する「性格診断」機能**により、資産全体の傾向を多角的に分析可能です。このページの保有銘柄一覧は、各項目でのソートに加え、銘柄コード/銘柄名でのテキスト検索、業種・口座種別・**証券会社**でのドロップダウンフィルタを組み合わせた柔軟な絞り込みが可能です。さらに、プライバシー保護のため、ページ上のすべての金額表示（評価額、損益など）をワンクリックで隠す「目隠し機能」も備えています。
- **指標ハイライト**: PERやPBRなどの指標が、設定ファイル(`highlight_rules.json`)の基準に応じて色付けされ、割安・割高が一目で分かります。
- **総合評価スコア**: 複数の指標（PER, PBR, ROE, 利回り, **連続増配**, **トレンド**, **フィボナッチ**, **RCI**）を総合的に評価した「スコア」を**最大15点**の星印で表示し、銘柄の魅力を多角的に判断できます。
    - **色分け表示**: 指標の種類に応じて星の色を分けて表示します。
        - **ファンダメンタルズ評価**（PER, PBR, ROE, 利回り, 連続増配）: **黄色**の星（★）
        - **トレンド評価**（移動平均線との乖離など）: **青色**の星（★）
- **ポートフォリオ特性分析 (Portfolio DNA & Risk)**: [強化]
    - **ポートフォリオのDNA**: 保有額の大きさを考慮した「加重平均」により、資産全体の平均PER、PBR、ROE、利回りを算出。
    - **初心者向け最適化**: 専門用語を「割安さ」「稼ぐ力」などの直感的な日本語に翻訳し、基準値に基づき信号機カラー（青・黄・赤）で状態を表示。
    - **データの信頼性チェック**: 各指標の「カバー率」を算出し、データ不足（70%未満）時は「⚠️参考値」として警告を表示することで、誤った判断を防止。
    - **分散度・リスク分析**: 特定銘柄への集中度を測る「HHI指数（カゴの分け具合）」と「上位5銘柄占有率」を算出。分散が効いているか、特定の銘柄に依存しすぎていないかを直感的に診断します。
- **ポートフォリオ性格診断 (Style Analysis)**: [強化]
    - **スタイル判定**: 保有銘柄の属性から「景気特性（守りに強い/景気に敏感）」「投資スタイル（割安/成長）」「時価総額（大型/中小型）」の比率を可視化。
    - **ひとこと診断（アドバイス）**: 分析結果に基づき、「現在は大型の割安株が中心の、どっしりと安定した構成です」といった、現状を要約した日本語の診断メッセージを表示します。
- **ポートフォリオ特性 (Radar Chart)**: [新機能]
    - **多角的な視覚化**: 「割安性」「収益性」「インカム」「クオリティ」「モメンタム」「分散度」「安全性」の7つの軸でポートフォリオのバランスを可視化します。
    - **DNA・トレンドの統合**: DNA分析によるPBR加重平均や、テクニカル評価（モメンタム）、集中リスク（HHI指数）をチャートの計算に統合。資産全体の「強み」と「弱み」を多角的に把握できます。
    - **ベンチマーク比較**: 市場平均（ベンチマーク）を点線で重ねて表示し、自分のポートフォリオが市場に対してどのような特性を持っているかをひと目で把握できます。
- **連続増配分析**: 過去10年間の配当データから**連続増配年数**（前期比で増配している年数）を自動で計算し、バッジで表示します。詳細な配当履歴はツールチップで確認できます。
- **銘柄追加・削除**: 銘柄コードを指定して、ポートフォリオの銘柄を管理します。複数銘柄の一括削除、および直近追加した銘柄10件のリスト表示・入力補助機能も搭載しています。
- **ソート機能**: ポートフォリオ一覧および分析ページの保有銘柄一覧の各項目をクリックすることで、データを昇順・降順に並び替えることができます。銘柄コードや銘柄名でのリアルタイムフィルタリング機能も利用可能です。
- **外部リンク**: 銘柄名や連続増配バッジをクリックすると、Yahoo!ファイナンスの該当ページを新しいタブで開きます。
- **クイック詳細リンク (Quick Links)**: [新機能] メインページの国内株式一覧に、Yahoo!ファイナンスの「適時開示」と「業績詳細」へ直接ジャンプできるリンクバッジを追加。下落時の原因調査を劇的に高速化します。
- **CSVダウンロード**: 表示しているポートフォリオ全体や、分析ページの保有銘柄一覧をCSVファイルとしてダウンロードできます。
- **購入・状況注目フラグ (Market Signal)**: 「質の高い銘柄」の買い時や、過熱によるリスクを自動判定し、バッジ（🔥, 🌀, ⚠️等）で表示します。
    - **長期投資対応**: 5〜10年スパンの投資家向けに、下落をチャンスと捉える「長期調整(🌀)」や、高値掴みを防ぐ「過熱気味(⚠️)」を導入。
    - **ダイヤモンド評価**: ファンダメンタルズが特に優れた銘柄（黄色星4つ以上）にはダイヤモンド（💎）が付与され、厳選された投資判断をサポートします。
- **分析シグナル履歴の永続化**: [新機能] 銘柄ごとのスコアや売買シグナル（注目、チャンス、長期調整等）を、その判定時点のスナップショットとしてDBに自動保存。過去の特定の日に「どの銘柄にどのようなシグナルが出ていたか」を後から分析できるようになりました。
- **資産履歴の自動記録**: 分析ページを開くたびに月次資産推移を保存する機能に加え、日次の株価・指標データを時系列で自動的にデータベースへ蓄積します。
- **月別配当分布 (予測)**: [新機能] 銘柄ごとの決算月と年間配当金に基づき、将来1年間の配当受け取りスケジュールを月別に可視化（棒グラフ）。キャッシュフローの把握を容易にします。
- **フロントエンドキャッシュ**: APIから取得した資産データをブラウザにキャッシュし、再読み込み時の表示を高速化します。
- **スマート・キャッシュ・フェッチ**: [強化] バックエンド側でメモリキャッシュに加え、SQLiteによる日次永続化キャッシュを導入。市場の「開始時刻」と「終了時刻」の両方を考慮し、ザラ場中や大引け後など、市場の状態変化に合わせて最適なタイミングでキャッシュを更新・再利用します。サーバー再起動後も取得済みデータを即座に応答し、開発や運用の体感速度を劇的に向上させます。
- **データ取得レポート**: [新機能] データ取得完了後に、所要時間、成功/失敗数、アセットタイプ別の内訳、更新時刻を画面上に表示。システムの動作状況を直感的に把握できるようになりました。

## 詳細な機能一覧

### 1. ポートフォリオ管理機能
- **資産の追加**
    - 国内株式、米国株式、投資信託を銘柄コード、ティッカー、ファンドコードでポートフォリオに追加する。
    - コードの形式から資産タイプ（国内株/米国株/投資信託）を自動で判別する。
- **資産の削除**
    - ポートフォリオから特定の資産を選択して一括で削除する。
- **資産情報表示**
    - 資産タイプごとにタブで表示を切り替える。
    - 各資産の最新情報を一覧で表示する（株価、前日比、時価総額など）。
    - 一覧の各項目でソート（並び替え）が可能。
    - 銘柄コードや名称で表示内容をフィルタリングする。
- **保有状況の管理**
    - 資産ごとに、複数の口座（特定口座, 一般口座, 新NISA, 旧NISA）の保有情報を登録・更新・削除できる。
    - 保有情報には「取得単価」と「数量」を記録する。
- **データ更新**
    - 「全件更新」ボタンで、ポートフォリオ内の全資産の情報を最新化する（クールダウン機能付き）。
- **CSVエクスポート**
    - 現在表示しているポートフォリオの一覧をCSVファイルとしてダウンロードする。

### 2. ポートフォリオ分析機能
- **サマリー表示**
    - ポートフォリオ全体の評価額、投資額、損益、損益率、年間配当合計などを表示する。
    - 金額表示のON/OFFを切り替えることができる。
- **グラフ表示**
    - 保有資産の構成比を円グラフで可視化する（業種別、口座種別、証券会社別、国別、配当構成）。
    - **ポートフォリオ特性（レーダーチャート）**: 「割安性」「収益性」「インカム」「クオリティ」「モメンタム」「分散度」「安全性」の7つの軸で資産全体のバランスを可視化。
    - **月別配当分布 (予測)**: 日本株・米国株の支払いパターンに基づき、将来の配当収入を月別に棒グラフで可視化。
- **保有銘柄一覧**
    - 全ての保有資産の詳細な情報を一覧で表示する（評価額、損益、損益率など）。
    - 銘柄名、業種、口座種別、証券会社、**シグナル**でフィルタリングが可能。
- **分析結果のCSVエクスポート**
    - 保有銘柄一覧をCSVファイルとしてダウンロードする。

### 3. データ取得・加工機能
- **スクレイピング（安定化・高速化対応済み）**
    - Yahoo!ファイナンスから以下の情報を取得する。
        - **国内株式**: 株価、企業情報（業種、決算月）、財務指標（PER, PBR, ROE, EPS）、配当情報（利回り, 過去配当履歴）。
        - **米国株式**: 株価、市場情報、財務指標（PER, EPS）、決算月。
        - **投資信託**: 基準価額、純資産総額、信託報酬。
    - **安定化対策**: `requests.Session` による接続維持、同時実行数の制限、銘柄内待機時間を導入し、アクセス制限（500エラー）を回避します。
    - **高速化対策**: キャッシュ存在時の待機スキップロジックにより、2回目以降の表示を高速化します。
- **スコアリング（国内株式のみ）**
    - PER, PBR, ROE, 配当利回り, 連続増配年数などの指標に基づき、独自の評価スコアを算出する。
- **注目シグナル判定**
    - テクニカル指標（RSI, RCI, フィボナッチ, 5日線等）とファンダメンタルズスコアを組み合わせ、買い場や過熱感を判定する。

## セットアップと実行方法

1. **リポジトリをクローンします。**
   ```bash
   git clone https://github.com/your-username/jp-stock-portfolio-app.git
   cd jp-stock-portfolio-app
   ```

2. **必要なPythonライブラリをインストールします。**
   ```bash
   pip install fastapi uvicorn python-multipart requests jinja2 beautifulsoup4 cachetools
   ```

3. **FastAPI開発サーバーを起動します。**
   ```bash
   uvicorn app:app --reload
   ```

4. **ブラウザでアクセスします。**
   Webブラウザを開き、 `http://127.0.0.1:8000` にアクセスしてください。

## next step
- **[改善]データ取得先のnext.js対応に伴う修正対応**: 調査＆設計して修正する
- **[bug]国内投資信託のデータ取得不正その２**: 純資産総額がundefined、信託報酬がundefinedになっている
- **[bug]米国株式のデータ取得不正その２**:市場がN/A, 現在値が全ての銘柄が49490になっている。前日比が全ての銘柄で、--- (0.00%)になっている。時価総額はN/Aになっている。
- **[改善]データ取得の障害や改善時に当日のデータがあっても強制上書きできる仕組みが欲しい**:現状の仕様確認と検討から。
- **[UI改善]ダークモードの選択ボタンがanalysis画面にしかない為、mainページ側でも選択可能にする**: 
    - 現状、ダークモード解除するために、analysisページに行かないといけない。各ページで対応できるようにしたい。
- **[UI改善]シグナル情報のアニメーションのブラッシュアップ対応**: 現在、アニメーションがやや単調になってしまっている。
    - デグレを起こさないことを最優先として、ブラッシュアップできることがないか？を検討する
- **[改善]日経平均先物の情報が正しく取得できていない**:
    - 現在は、日経平均先物1限月の値を取得しているが、この値は、日経平均先物とは違う情報だったので修正する
- **パフォーマンス改善**: 登録銘柄数が数百件規模に増えた場合にも、軽快な動作を維持するための改善。
- **UI/UXのさらなる向上**: ユーザーからのフィードバックに基づく継続的な改善。
- **テストコードの拡充**: より安定したアプリケーションのための単体テスト・結合テストの追加。

---
### 現在の課題 (2026/04/17 時点)
- 銘柄数が多い場合、初回（または最新確定値がない場合）のデータ取得に数分程度の時間を要する（サーバー制限回避のための仕様）。

---

## 開発者向けガイドライン

### 1. フロントエンドのキャッシュ対策
JavaScript (`static/js/`) や CSS (`static/css/`) を修正した場合、ブラウザが古いキャッシュを読み込み続けることがあります。変更を確実に反映させるため、必ず呼び出し元の HTML ファイル内のバージョン番号（クエリパラメータ）をインクリメントしてください。

- **対象ファイル**: `templates/index.html`, `templates/analysis.html`
- **修正例**:
  ```html
  <!-- v=3.0 を v=3.1 に更新 -->
  <script src="/static/js/analysis.js?v=3.1"></script>
  ```

---

## 対応済みの課題

### 月別配当分布（予測）説明文のダークモード視認性改善 (2026/04/19 対応)
- **内容**: 分析ページの「月別配当分布 (予測)」下部にある算出仕様の説明文について、ダークモード適用時に背景色と文字色が同化して読めなくなる問題を解消しました。
- **原因の特定**: 表示ボックス `.dividend-spec-note` の背景色がライトモード用の色（`#f8f9fa`）に固定されていた一方で、文字色にテーマ依存の変数が混在していたため、「白背景に白文字」の状態が発生していました。
- **改善点**: 
    - ハードコードされた色指定を廃止し、背景色に `var(--header-bg-color)`、文字色に `var(--text-color)` を採用。
    - 補足説明（`small`）の色も `var(--text-muted)` へ統一し、テーマ切り替えに完全追従する設計に刷新しました。
- **キャッシュ対策**: 変更を即座に反映させるため、HTML側のCSSバージョンを `v=3.1` に更新しました。

### 判定不能シグナルのダークモード視認性改善 (2026/04/17 対応)
- **内容**: ダークモード利用時に「判定不能（🔘）」バッジの文字が背景と同化して読めなくなる不具合を解消しました。
- **原因の特定**: 表示テーマ `theme-unreliable` のCSS定義が欠落しており、デフォルトの濃い黒文字がダークモードの暗い背景に埋没していました。
- **改善点**: 
    - `style.css` に `theme-unreliable` を新規定義し、ライト/ダーク両モードで最適な背景色と文字色を設定。
    - 他のシグナルと同様に「三層サンドイッチ構造」を適用し、低速のシマーアニメーションを付与してデザインの統一感を図りました。
- **キャッシュ対策**: CSS/JSの更新を反映させるため、HTML側のバージョンを `v=3.0` に更新しました。

### 分析ページのUI最適化とダークモード視認性の向上 (2026/04/16 対応)
- **内容**: 分析ページのUIをより洗練させ、情報のノイズを排除するとともに、ダークモード時の操作性を改善しました。
- **市場指標の非表示化**: 分析ページを「自身の資産分析」に特化させるため、メインページにのみ必要だった日経平均やTOPIX等の表示を削除しました。これにより、自身の資産データに集中できる整理されたレイアウトを実現しました。
- **ダークモードの配色修正**: チャート切り替えボタンがダークモードで「白背景に白文字」になり視認性が失われていた不具合を解消しました。CSS変数による背景色管理（`--button-bg-color`）を導入し、ライト/ダーク両テーマで最適なコントラストが保たれるように設計を刷新しました。
- **キャッシュ対策の徹底**: CSS/JSの更新を反映させるため、HTML側のバージョンを `v=2.9` に更新しました。

### 分析用ノートブックのデータ抽出・フラット化ロジックの修正 (2026/04/15 対応)
- **内容**: `notebooks/portfolio_analysis.ipynb` において、DBからデータを抽出する際にほとんどの項目が `NaN` になっていた不具合を修正しました。
- **原因の特定**: 保存されている JSON の階層構造（`indicators` ネストの有無）やキー名（`change_pct` vs `change_percent`）の誤解、およびスコア合計値が DB に直接保存されていない（内訳のみ）という仕様との乖離が原因でした。
- **改善点**: 
    - キー名を最新の DB 仕様（`change_percent`, `yield` 等）に合わせました。
    - `score_details` から `fundamental_score` および `technical_score` を動的に計算するロジックを実装しました。
    - `dividend_history` から「連続増配年数」を再計算する処理を追加し、分析の精度を向上させました。
- **検証**: 実際のデータベース `./portfolio_history.db` を用いて、約 2,500 レコードが正常にフラット化され、各指標が数値として正しく取得できることを確認済みです。

### フィボナッチ算出期間（日数）の可視化 (2026/04/15 対応)
- **内容**: フィボナッチリトレースメントの算出に「どの期間のデータが使われているか」を透明化するため、UI上で算出日数を確認できる機能を実装しました。
- **UIの改善**: 国内株式テーブルのフィボナッチ数値にツールチップを追加。マウスホバーで「算出期間（日数）」「直近高値」「直近安値」を表示するようにし、投資判断の根拠を明確にしました。
- **データ構造の拡張**: バックエンドの計算ロジックを修正し、実際に計算に使用された有効な終値の件数を動的に取得・伝播させる仕組みを構築しました。これにより、キャッシュデータと最新データの違いも正確に把握可能になりました。
- **整合性の維持**: 既存の総合評価スコアやシグナル判定ロジックへの影響をゼロに抑えつつ、情報の詳細度のみを向上させました。

### シグナルアニメーションの完全復旧と三層レイヤー構造の導入 (2026/04/14 対応)
- **内容**: シグナルバッジ（チャンス、過熱、ピークアウト等）のアニメーションが停止、あるいは背景に隠れて見えなくなっていた不具合を抜本的に解決しました。
- **三層サンドイッチ構造**: バッジ内部を「背景層」「アニメーション層 (z-index:1)」「文字層 (z-index:2)」に物理的に分離。これにより、不透明な背景色に遮られることなく、かつ文字の可読性を100%維持した状態でアニメーションを可視化しました。
- **物理移動 (translateX) への統一**: ブラウザの描画最適化による「間引き」を防ぐため、すべてのシグナルでダイヤモンド（💎）と同様の「光が走り抜ける物理移動」を採用。GPU加速により、テーブル内でも滑らかに動作します。
- **重要度別の速度設定**: ピークアウト（1.2s）から長期調整（4.0s）まで、シグナルの緊急度に応じた速度の差別化を実装しました。
- **キャッシュ対策の徹底**: HTML側で読み込むCSS/JSのバージョンを `v=2.6` に更新しました。

### シグナル表示の整合性確保とアニメーションの完全復旧 (Single Theme Model) (2026/04/13 対応)
- **内容**: 複数のCSSクラスが干渉し合い、売却シグナルの背景色が正しく表示されない不具合（赤やオレンジであるべきバッジが、優秀銘柄のダイヤモンド属性によって水色に上書きされる等）を抜本的に解決しました。
- **排他的テーマ適用モデル (Single Theme Model)**: 複数のクラスを重ね掛けしてCSSの優先順位に委ねる従来のアプローチを廃止し、JS側で状態を確定させてから「唯一のテーマクラス（`theme-***`）」を付与する設計に刷新しました。これにより、表示の100%制御が可能になり、デグレを物理的に防ぐ構造を実現しました。
- **アニメーションの復旧**: UI刷新時に欠落していた `@keyframes` 定義（`signal-shimmer`, `pulse-red`）をCSSに追加し、バッジの点滅や輝きといった視覚効果を完全に復旧させました。
- **リスク優先の視認性**: 売却シグナル（警告）発生時は、銘柄がダイヤモンド（💎）であっても警告色（赤・オレンジ）を背景色として100%優先。ダイヤモンド属性はアイコン内に `💎` を含めることで、情報の欠落なしにリスクを正しく伝えるUIに調整しました。
- **キャッシュ対策の徹底**: HTML側で読み込むCSS/JSのバージョンを `v=2.0` にインクリメントし、全ユーザーに最新のスタイルが即座に反映されるように措置しました。

### アプリケーションUI/UXの大規模モダン化とシグナル整合性の刷新 (2026/04/12 対応)
- **内容**: プロフェッショナルな金融分析ツールとしての外観と操作性を追求し、UI/UXを全面的に刷新しました。
- **レイアウトの進化 (Bento Grid)**: 分析ページのサマリーエリアを「ベントー・グリッド」形式に再構築。2列配置により情報密度を最適化し、一画面でより多くの指標を俯瞰可能にしました。
- **全画面ダークモードの導入**: ユーザーの好みに応じてテーマを切り替えられるダークモードを実装。`localStorage` による設定の永続化と、Chart.js の動的なテーマ追従（文字色・グリッド線の自動更新）を実現しました。
- **ローディング体験の向上 (Skeleton UI)**: データ取得中にコンテンツの枠組みがぼんやりと光る「スケルトンUI」を導入。従来のテキスト表示に比べ、体感的な待ち時間を大幅に短縮しました。
- **データ視認性のプロ仕様化**:
    - **等幅フォント (Monospace)**: 数値セル専用の `.numeric` クラスを作成し、SFMono/Consolas 等の等幅フォントを適用。桁数が揃うことで上下行の比較効率を劇的に向上させました。
    - **グラスモフィズム**: テーブルヘッダーをスティッキー（固定）化し、半透明のぼかしエフェクトを適用。スクロール時の洗練された見た目と利便性を両立しました。
- **シグナル・視覚効果の高品質化と整合性確保**:
    - **コントラスト最適化**: バッジの文字を白から「濃い黒文字」ベースへ移行し、アニメーション背景の上でも確実に読めるように改善。
    - **不整合の根絶**: 「長期調整」という名称が購入・売却の両側で使われていた問題を解消。売却側を「トレンド崩壊」に、購入側を「＋長期調整中」に改称し、色と意味を一対一で同期させました。
    - **ピークアウト強化**: 最重要リスクサインを「鮮やかな赤背景＋黒文字＋白グロー」に固定し、視認性を最大化しました。
- **効果**: 既存の強力な分析ロジックを損なうことなく、現代的なSPA（シングルページアプリケーション）としての高い完成度を実現しました。

### 下落原因調査の迅速化（外部リンクの強化） (2026/04/04 対応)
- **内容**: 株価急落時などの原因調査を効率化するため、メインページの国内株式一覧に Yahoo!ファイナンスの「適時開示」および「業績詳細」へのダイレクトリンクを追加しました。
- **リスク回避**: 通信負荷増大によるアクセス制限（403エラー）のリスクを避けるため、自動スクレイピングではなく、ユーザーが必要な時にワンクリックで詳細を確認できる「リンク強化」という最小構成を採用しました。
- **利便性の向上**: 銘柄名クリック後の複数ステップ（トップ→タブ選択）を排除し、1クリックで目的の情報へ辿り着けるように改善しました。
- **ユースケースの最適化**: 購入判断を行うメインページのみにリンクを配置し、分析ページはポートフォリオのバランス確認に集中できるよう従来のシンプルな表示を維持しました。

### ポートフォリオ分析サマリーの30日前比較（MoM）へのアップグレード (2026/04/04 対応)
- **内容**: 分析ページのサマリー項目（総評価額、総損益、年間配当）について、これまでの「前月末との比較」から「30日前（またはそれに最も近い過去データ）との比較」へと刷新しました。
- **DB構造の日次化**: `portfolio_summary_history` テーブルの主キーを月次から日次 (`snapshot_date`) に変更。1日単位での資産推移を記録可能にしました。
- **インテリジェント比較**: 30日前にアプリを開いていなかった場合でも、指定日以前で最も新しいデータを自動特定して比較するロジックを導入しました。
- **視認性の向上**: UI上の比率表示にツールチップを追加し、「30日前との比較」であることを明記しました。

### 分析スナップショット保存の最適化とログ抑制 (2026/04/04 対応)
- **内容**: 銘柄数が多い場合のログ氾濫（DEBUGログ）と不要なDB上書き保存を抑制しました。
- **Smart Snapshot Update**: DBキャッシュ利用時、保有状況や分析結果に変化がない場合は物理的なDB保存をスキップするように最適化。
- **ログ抑制**: 銘柄ごとのスコア詳細、キャッシュヒット、保存完了の DEBUG ログを削除。100件以上の銘柄がある環境でも、サーバーログが溢れないように改善しました。
- **効果**: アクセス時のレスポンスが向上し、ログの可読性が劇的に改善されました。

### ポートフォリオ分析サマリーの先月比（MoM）可視化と履歴保存 (2026/04/02 対応)
- **内容**: 分析ページのサマリー項目（総評価額、総損益、年間配当）について、前月末（直近の過去月）の状態と比較した変動率（MoM）を表示する機能を実装しました。
- **履歴データの最適化**: 銘柄ごとの詳細履歴とは別に、ポートフォリオ全体の合計値を月次で記録する `portfolio_summary_history` テーブルを新設。数年スパンでの長期的な資産推移の抽出・比較を高速に行える基盤を構築しました。
- **インテリジェントな表示**: フィルタ適用時は全体値との不正確な比較を避けるため比率を非表示にするなど、データの正確性を重視したUI制御を導入しました。

### 市場指標（日経平均・TOPIX・先物）の騰落表示の洗練化とUI統一 (2026/04/11 対応)
- **内容**: 主要な市場指標（日経平均、TOPIX、先物）の騰落表示について、視認性を大幅に向上させ、アプリ全体のデザインと統一しました。
- **UIの統一**: 騰落色をポートフォリオ分析画面の損益表示と同期させ、「上昇・プラス＝緑色（成功・増加）」、「下落・マイナス＝赤色（警告・減少）」に統一。直感的な判断をサポートします。
- **視認性の向上**: 前日比のパーセント表示に必ず `+` または `-` 符号を付与するロジックを実装。また、前日比だけでなく「前週比」「前月比」についても正負に応じた色付けを適用しました。
- **保守性の確保**: CSS変数 (`--up-color`, `--down-color`) を導入し、将来的なデザイン変更やダークモード対応に柔軟に対応できる設計に刷新しました。

### 市場指標サマリー（日経平均・TOPIX・先物）の可視化 (2026/04/01 対応)
- **内容**: 主要な市場指標の動きをリアルタイムおよび時系列で把握できる機能を実装しました。
- **対象指標**: 日経平均株価 (`998407.O`)、TOPIX (`998405.T`)、日経平均先物1限月 (`5040469.O`)。
- **多角的分析**: 現在値と前日比に加え、DBに蓄積された日次データから「前週比 (WoW)」および「前月比 (MoM)」を自動算出。
- **週末対応**: 先物のナイトセッション終了（土曜朝 06:00）を考慮したキャッシュ判定ロジックを導入。土日も最新の先物終値を確認しつつ、個別株の不要なスクレイピングを回避する効率的な通信を実現しました。
- **効果**: 自分のポートフォリオだけでなく、市場全体の地合いや予測（先物による月曜気配など）を一つの画面で完結して把握できるようになりました。

### 保有銘柄情報の履歴永続化（復元） (2026/03/31 対応)
- **内容**: 以前の開発でプライバシー保護のために除外されていた保有情報（買付単価、数量、メモ、証券会社、口座種別）を、日次履歴 DB (`daily_stock_history`) の各銘柄スナップショット内に `holdings_snapshot` として復元しました。
- **全アセット対応**: 国内株式だけでなく、米国株式や投資信託についても、その時点の保有状況が日次で保存されるように拡張しました。
- **効果**: 「いつどの銘柄をいくらで持っていたか」の履歴がDBに蓄積されるようになり、将来的に過去の資産推移や売買タイミングの精緻な分析が可能になりました。

### 月別配当分布（予測）の仕様説明の追加 (2026/03/31 対応)
- **内容**: 分析ページの「月別配当分布 (予測)」チャートにおいて、どのような計算根拠で月別の配当額が算出されているかをユーザーが把握できるよう、チャート直下に算出仕様（日本株：3/9ヶ月後、米国株：3ヶ月おき等）の説明を追加しました。
- **効果**: 実際の入金月との乖離に対する疑問を解消し、データの信頼性と納得感を向上させました。

### 分析ページの月別配当分布グラフの目隠し対応 (2026/03/30 対応)
- **原因**: 「金額を隠す」ボタンをクリックした際のイベントリスナーにおいて、テーブルやサマリー、歴史グラフ（資産・配当推移）の再描画は行われていましたが、月別配当分布グラフ（および円グラフ等）を含む `renderCharts` 関数の呼び出しが漏れていました。そのため、初期描画後にチェックを切り替えても、これらのグラフの目盛りやツールチップが最新の目隠し状態に更新されない不具合が発生していました。
- **対応**: 
  - **再描画トリガーの追加**: `static/js/analysis.js` の `toggleVisibilityCheckbox` イベントハンドラに `renderCharts(filteredHoldingsData)` を追加し、全グラフを一括して最新の `isAmountVisible` 状態に基づいて再描画するように改善しました。
  - **UIの一貫性確保**: これにより、資産推移グラフと同様に、月別配当分布グラフの Y 軸目盛りも `isAmountVisible` が `false` の際には正しく `***円` とマスク表示されるようになりました。
- **効果**: プライバシー保護機能の適用範囲を視覚化レイヤー全体に拡大し、ユーザーが安心してポートフォリオの相対的な構成（棒グラフの高さなど）を確認できるようになりました。

### 分析シグナル履歴のDB永続化 (2026/03/29 対応)
- **原因**: 銘柄ごとの「注目」や「チャンス」といったシグナルは、画面表示のたびに最新データから再計算されており、過去にどのタイミングでシグナルが発生し、その後株価がどう推移したかを後から検証（バックテスト）することが困難でした。
- **対応**: 日次履歴 DB (`daily_stock_history`) のデータ構造を拡張し、計算済みの分析結果を「スナップショット」として保存する機能を実装しました。
  - **情報の統合**: スコア、詳細内訳、売買シグナル、データの信頼性フラグを一つのオブジェクトとして JSON カラム内に格納。
  - **ロジックの共通化**: `app.py` 内に `_enrich_stock_data` 関数を導入。全件更新、個別取得、銘柄追加のすべてのルートで同一の分析ロジックが走り、かつ DB が最新の状態に更新される設計に刷新しました。
  - **プライバシー保護**: DB 保存時には `portfolio.json` にあるユーザー固有の保有情報（取得単価や数量）を自動的に除外し、統計・分析データのみを蓄積するように配慮しました。
- **効果**: 過去の判定履歴を SQL や Pandas 等で直接抽出・分析できるようになり、独自の投資ロジックの有効性を客観的に評価する基盤が整いました。

### 月別配当分布（概算モデル）の導入 (2026/03/28 対応)
- **原因**: 「どの月にいくら配当が入るか」を把握したいというニーズに対し、当初は外部サイトの詳細配当スケジュール（JSONの深い階層）に依存する設計を検討していましたが、これはサイトの仕様変更によるアプリ全体の動作停止（デグレ）リスクが極めて高い状態でした。
- **対応**: 堅牢性と実用性を両立させるため、安定して取得できている「決算月」と「年間配当」に基づく「概算配当分布モデル」を実装しました。
  - **ロジックの最適化**: 日本株（決算月+3/+9ヶ月の年2回）と米国株（3ヶ月おき年4回）の一般的な支払いパターンをシミュレート。
  - **リスク排除**: スクレイピング対象を増やさないことで、アクセス制限リスクを抑制。
  - **UI/UX**: 他の分析グラフとデザインを統一し、目隠し機能にも完全対応。
- **効果**: キャッシュフローの目安を安全かつ高速に可視化できるようになりました。

### ザラ場中のキャッシュ判定ロジックの適正化 (2026/03/26 対応)
- **原因**: 従来のキャッシュ鮮度判定が「直近の大引け時刻（15:30等）」のみを基準にしていたため、前日の大引け後に取得されたデータがある場合、翌日の午前中（前場）であっても「大引け時刻より後のデータである」と誤判定され、古いキャッシュが採用されて当日株価が取得できない問題が発生していました。
- **対応**: 判定基準を「最新の市場イベント時刻」へと刷新しました。
  - **開始時刻の導入**: `highlight_rules.json` に各市場の開始時刻（`open_time_jst`）を追加。
  - **動的な基準時刻算出**: 「直近の開始時刻」と「直近の終了時刻」のうち、より現在に近い過去の時刻を基準（`threshold_time`）とする `get_cache_threshold_time` 関数を実装。
  - **効果**: 10:32（前場中）にアクセスした場合、当日 09:00 以降のデータがなければスクレイピングが実行されるようになり、ザラ場中のリアルタイム性が確保されました。また、寄付前や夜間・週末などは引き続き確定値キャッシュが有効に機能し、不要な通信を抑制します。

### インテリジェント・キャッシュ制御の刷新とDB最適化 (2026/03/25 対応)
- **原因**: 従来の「当日日付のみ」のキャッシュ判定では、深夜（01:00等）や土日に「当日分が空」であるために不要なスクレイピングが発生し、Yahoo!ファイナンスへの負荷とアクセス制限のリスクを高めていました。また、大量のメモリキャッシュヒットが並列制限をバイパスしていたため、取得時間の統計が不自然（短すぎる）になっていました。
- **対応**: 「データの鮮度」に基づくインテリジェントな制御へ刷新しました。
  - **鮮度判定の導入**: 「DB内の最新データの更新日時 ≧ 直近の市場確定時刻（大引け）」を満たせば、日付を問わずキャッシュを再利用するロジックを実装。
  - **正確な営業日判定**: 日本株（土日休み）と米国株（JSTの日月休み、土曜朝クローズ）の時差と営業日を完全に考慮した `get_last_market_close_time` を実装。
  - **DB検索の高速化**: 履歴データの増大に備え、`daily_stock_history` テーブルに複合インデックス `(code, date)`を追加。全銘柄の最新データを日付を問わず瞬時に取得可能にしました。
  - **並列制御の適正化**: キャッシュヒット時も同時実行数（セマフォ）の管理下におくよう整理. 安全性を担保しつつ、キャッシュ時は待機をスキップすることで高速なレスポンスを維持しました。
- **効果**: 深夜・早早朝や週末の無駄な通信がゼロになり、サーバー再起動後も常に「その時点で可能な最新の確定値」が即座に表示されるようになりました。

### データ取得レポート（統計情報）の可視化 (2026/03/24 対応)
- **原因**: 銘柄データの取得にかかった時間や成功/失敗の数がサーバーログにしか出力されず、ユーザーが取得の質やキャッシュの効果を実感しにくい状態でした。
- **対応**: 通信プロトコルと UI の両面を拡張しました。
  - **APIの拡張**: `/api/stocks` および `/api/portfolio/analysis` のレスポンスに `metadata` フィールドを追加し、統計情報をフロントエンドへ伝播するようにしました。
  - **取得レポートUI**: メインページ and 分析ページにレポート表示領域（`.update-report`）を追加。取得完了時に統計情報を即座に表示するようにしました。
  - **防衛的実装**: フロントエンド側で新旧のレスポンス形式を吸収するガード処理を徹底し、既存のキャッシュデータがあってもデグレが発生しない安全な移行を実現しました。
  - **視認性の向上**: 失敗がある場合の強調表示や、分析ページでの「取得対象数」の事前表示により、システムの透明性を高めました。

### 分析ページの表示不具合の修正 (2026/03/24 対応)
- **原因**: 上記「データ取得レポート」の実装過程で、`analysis.js` 内のデータ抽出ロジックが新形式に対応できておらず、またコード編集ミスにより必要な変数宣言が欠落したため、`ReferenceError` が発生して描画が停止していました。
- **対応**: 
  - **データアクセスの正規化**: ラップされた新形式と旧形式のどちらからでも正しく `holdings_list` を抽出できるよう防衛コードを追加しました。
  - **基盤コードの復元**: 欠落していたグローバル変数および Chart インスタンスの宣言を復元し、正常な動作を回復しました。

### クールダウン制限の撤廃とスマートキャッシュへの完全移行 (2026/03/23 対応)
- **原因**: 以前は過度なスクレイピングによるサーバー負荷とアクセス制限を避けるため、フロントエンドとバックエンドの両方で強力な「10分間の待機時間（クールダウン）」を設けていました。しかし、スマートキャッシュ（DB永続化）が導入された後もこの制限が残っていたため、ページ遷移のたびに「リクエストが多すぎます」というエラーが出たり、再試行のループが発生したりしてUXを損なっていました。
- **対応**: DBキャッシュが十分に信頼できる状態になったため、時間ベースの一律な制限を撤廃しました。
  - **バックエンド制限解除**: `app.py` の 429 エラー返却ロジックを無効化。
  - **フロントエンド即時取得**: `stateManager.js` の待機判定を廃止し、ページ表示時に常に最新データを問い合わせるよう改善。
  - **スマートキャッシュ活用**: バックエンド側で「市場確定後ならDB採用」「日中も1時間以内ならDB採用」という既存の賢い判定に処理を委ねることで、ユーザーの待ち時間をゼロにしつつ、Yahoo!ファイナンスへの不要な通信を完全に防ぐ設計に統合しました。
  - **エラー表示の整理**: 通信制限による429エラーが発生しても、アラートを表示せずにキャッシュデータを優先して表示し続けることで、ユーザー体験を途切れさせないように改善しました。

### 時系列資産データの永続化とスマートキャッシュの実装 (2026/03/22 対応)
- **原因**: 修正作業中にサーバーが再起動されるとメモリ上のキャッシュが消え、再度 Yahoo!ファイナンスへの低速なスクレイピング（1銘柄ずつ待機）が発生し、開発効率とユーザー体験が低下していました。また、過去の株価推移を分析するための蓄積基盤がありませんでした。
- **対応**: SQLite DB (`daily_stock_history` テーブル) を活用した、JST基準の永続化キャッシュ機構を構築しました。
  - **インテリジェント・キャッシュ**: 市場確定時刻（大引け時刻）を `highlight_rules.json` で資産別に設定可能に。確定時刻以降であれば DB のデータを「本日の確定値」として優先利用し、スクレイピングを完全にスキップします。
  - **時系列データの蓄積**: 取得成功時にその日の最新データを DB に保存（1日1銘柄1レコード）。これにより、サーバー再起動後も即座にデータが復元され、かつ将来的な時系列分析の基盤が整いました。
  - **保存前バリデーション**: 通信エラーやデータ欠損がある不完全なデータが DB に保存されないよう厳格なチェックを実装し、評価の信頼性を維持しました。
  - **開発効率の向上**: 15:30 以降であれば、何度コードを修正・再起動しても全銘柄が瞬時に表示されるようになり、開発サイクルが劇的に高速化しました。

### 銘柄情報取得処理のサマリログ表示の実装 (2026/03/21 対応)
- **原因**: 銘柄情報の取得にどの程度の時間がかかっているか、またアセットタイプごとの内訳や成功/失敗数が正確に把握できない状態でした。
- **対応**: データ取得の内部共通関数 `_get_processed_asset_data` に計測・集計ロジックを実装しました。
  - **サマリログ**: 処理終了時に, 所要時間, 対象総件数, 成功/失敗数, アセットタイプ別内訳（国内株/投信/米国株）を1行でサーバーログに出力するようにしました。
  - **目的**: パフォーマンスの可視化と、スクレイピング制限等のトラブル発生時の迅速な状況把握を可能にしました。

### 相反するシグナルの表示抑制と優先順位の適正化 (2026/03/20 対応)
- **原因**: 「購入シグナル（注目・チャンス）」と「売却シグナル（ピークアウト等）」が独立して計算されていたため、特定の局面で両方のバッジが同時に表示され、投資判断に混乱を招くケースがありました。また、抑制ロジックがコード内に分散しており、メンテナンス性に課題がありました。
- **対応**: シグナル統合関数 `reconcile_signals` を導入し、優先順位に基づき一つの有効なシグナルを表示するように刷新しました。
  - **リスク優先**: 短期的な反転下落リスクを示す「ピークアウト(Lv2)」または「過熱気味(Lv1)」が発生している場合、購入シグナルを抑制して警告を優先表示します。
  - **長期調整の統合**: 「長期調整(Lv3)」発生時は、購入シグナルの名称に「＋長期調整」が統合される仕様を活かし、購入側を優先表示します。
  - **ロジックの集約**: 全件更新、個別取得、銘柄追加の全エンドポイントでこの共通関数を適用し、表示の整合性とコードの健全性を高めました。

### データ取得エラーおよび欠損値のハンドリング改善 (2026/03/19 対応)
- **原因**: Yahoo!ファイナンスのアクセス制限(403)や一時的なサーバーエラー(500)が発生した際、ユーザーには一律で「データ取得に失敗しました」としか表示されず、原因の特定や対策（待機など）が困難でした。また、一部の指標が取得できない（N/A）場合に、総合評価スコアが不当に低く算出され、優良銘柄を見落とすリスクがありました。
- **対応**: 通信・計算・表示の全レイヤーで、情報の透明性を高める改善を行いました。
  - **詳細エラーの伝播**: HTTP ステータスコードをフロントエンドまで保持。403エラー時には「15分ほど待機してください」といった具体的なヒントを表示するように改善。
  - **即時中断ロジック**: 403エラーを検知した場合、アクセス拒否の悪化を防ぐため、サーキットブレーカーを即座に発動（更新を中断）するように強化。
  - **判定不能シグナル (🔘)**: 重要指標（PER, PBR, 利回り）が欠損している場合に「判定不能」バッジを表示。データ不足による「偽の低スコア」と「真の低評価」を区別可能に。
  - **UIによる可視化**: N/A セルのグレーアウト、スコア（星評価）横への警告アイコン(⚠️)と欠損項目のツールチップ表示を実装し、評価の信頼性を直感的に把握できるよう改善。
- **原因**: Yahoo!ファイナンスの仕様変更（指標データの文字列化やHTML構造の変化）により、従来のJSON解析のみでは決算月が取得できず「N/A」となるケースが発生していました。
- **対応**: 通信負荷を一切増やすことなく、取得の確実性を高める「多層的抽出戦略」を実装しました。
  - **3段階の探索**: 1.指標データ(JSON)、2.HTMLタグ(正規表現)、3.配当基準日(メタデータ) の順で決算月を探索するフォールバック構造を構築. 
  - **確実性の向上**: 画面上に表示されているHTML構造を直接解析対象に加えることで、サイトの見た目が変わらない限り確実に取得できるよう改善しました。
  - **米国株対応**: 米国株についても同様のフォールバックを導入し、日付形式（YYYY/MMや和暦形式）の変動に柔軟に対応しました。

### スクレイピングの安定化と高速化 (2026/03/16 対応)
- **安定化**: Yahoo!ファイナンスのアクセス制限（500エラー）を回避するため、通信基盤を抜本的に強化。
  - **Session 導入**: `requests.Session` を採用し、接続を維持（Keep-Alive）することでサーバー負荷を軽減。
  - **徹底した負荷分散**: 同時実行数を「1」に制限し、さらに国内株の「株価取得」と「配当取得」の間に1秒の待機を設けることで、機械的なバーストアクセスを完全に解消。
  - **ブラウザ偽装の最適化**: 最新のブラウザヘッダー構成への更新と、リトライ間隔の延長（10秒）により、遮断されにくい堅牢な通信を実現。
- **高速化**: 「安全な低速取得」と「快適な高速表示」を両立させるロジックを実装。
  - **スマート待機**: メモリ上にキャッシュがある場合は、意図的な待機時間や同時実行制限をスキップして即座に応答。これにより、分析ページなどの2回目以降の表示速度が劇的に向上しました。

### 注目フラグの表示不正の修正と用語の統一 (2026/03/09 対応)
- **対応**: 「高確信」バッジの表示条件とメッセージの不整合を解消し、システム全体の用語を最新のポリシーに統一しました。
  - **閾値の適正化**: ダイヤモンド判定（高確信）の閾値が設定ファイル(4)とコード(6)で乖離していた問題を修正し、4点（黄色星4つ以上）に統一しました。
  - **動的メッセージ化**: 判定理由のテキストを「高確信(ファンダN点以上)」と動的に生成するように変更し、設定変更が即座にUIに反映されるようにしました。
  - **用語の完全統一**: バックエンドのフォールバック値やコメントに残っていた旧用語（利確注意、損切検討など）を、最新の「過熱気味」「長期調整(🌀)」等に完全に置き換えました。

### 売却・過熱シグナルの追加とフィルタの長期投資家向け最適化 (2026/03/07 対応)
- **対応**: 5〜10年スパンの長期投資家の視点に基づき、売買タイミングの可視化とフィルタ機能を抜本的に刷新しました。
  - **シグナルの再定義**: 従来の購入シグナルに加え、利確や警戒を促す「過熱気味(⚠️)」「ピークアウト(🚨)」、および長期的な安値圏を示す「長期調整(🌀)」を追加。
  - **損切り概念の排除**: 長期保有を前提とし、下落を「仕込みのチャンス」と捉えるようアイコン（📉→🌀）と文言を調整。
  - **長期投資特化型フィルタ**: 「厳選・押し目 (💎+🔥)」「厳選・安値圏 (💎+🌀)」「過熱・見送り (⚠️以上)」の3項目に整理。
  - **ダイヤモンド判定の独立**: 銘柄の質（黄色星4つ以上）をシグナルから独立させ、優良株の安値放置を確実にキャッチできるロジックへ強化。

### ポートフォリオ特性 (Radar Chart) の7軸拡張とDNA・トレンド統合 (2026/03/06 対応)
- **対応**: ポートフォリオの「強み」と「弱み」をより多角的に可視化するため、レーダーチャートを大幅に強化しました。
  - **7軸構成への拡張**: 従来の6軸に、資産全体の勢いを示す「モメンタム（テクニカル評価）」を追加。
  - **DNA分析との高度な連動**: 割安性軸にPERだけでなく「PBR加重平均」を統合し、分散度軸にTop5占有率だけでなく「HHI指数（集中度）」を組み合わせることで、分析結果の一貫性と精度を向上させました。
  - **評価ロジックの刷新**: 各指標の正規化処理を見直し、市場平均（ベンチマーク）と比較した際のバランスをより直感的に把握できるよう改善。

### 分析ページの保有銘柄一覧の適正化 [UI改善] (2026/03/03 対応)
- **対応**: 分析ページの保有銘柄一覧において、表記の適正化と不要な項目の整理を行いました。
  - **名称変更**: 「現在値 (外貨)」という表記を、円貨・外貨を問わず適切な「現在値」に変更しました。
  - **項目整理**: 分析において不要な「フィボナッチ」および「RCI(26)」の表示を非表示にし、一覧性を向上させました。
  - **CSV連動**: CSVエクスポートについても画面表示と項目を一致させ、データの整合性を確保しました。

### 配当分析の精緻化（配当対象資産の可視化） [UI改善] (2026/03/02 対応)
- **対応**: 分析ページのサマリー欄に「配当対象資産の評価額」を追加しました。
  - **計算根拠の明確化**: 投資信託（再投資型）などの無配資産を除いた、実際に配当金を生んでいる資産の合計額を明示。
  - **利回り把握の向上**: 表示されている「配当利回り（現在値）」が、どの程度の資産規模に対して算出されているかを直感的に把握可能に改善しました。


### 分析ページへの注目フラグフィルタの追加 [UI改善] (2026/03/01 対応)
- **対応**: 分析ページの保有銘柄一覧において、システムが「買い時」と判定している銘柄を素早く抽出できるフィルタ機能を追加しました。
  - **多段階フィルタ**: 「注目以上 (🟡/🔥/💎)」「チャンス以上 (🔥/💎)」「ダイヤモンド (💎)」の3つのレベルで絞り込みが可能。
  - **サマリー連動**: フィルタを適用すると、絞り込まれた銘柄のみで合計評価額、損益、利回りなどのサマリー数値が自動的に再計算されます。
  - **複合検索**: 既存の業種フィルタや証券会社フィルタと組み合わせて使用でき、保有株のリバランスや買い増し判断を強力にサポートします。

### 購入注目フラグ (Buy Signal) のUI刷新 [UI改善] (2026/02/28 対応)
- **対応**: 従来の絵文字アイコンから、モダンなバッジ形式のUIにアップデートしました。
  - **視視性向上**: 「注目」「チャンス」「高確信」といったテキストラベルとアイコンを組み合わせ、意味を直感的に把握可能に。
  - **レベル別デザイン**: 🟡（注目）、🔥（チャンス/パルスアニメ）、💎（厳選/シマーエフェクト）の3段階で色分けとアニメーションを適用。
  - **分析ページ統合**: ポートフォリオ分析ページの保有銘柄一覧にもバッジを表示し、保有株の買い時を特定しやすく改善。

### ポートフォリオ特性 (Radar Chart) の安全性ロジック刷新 (2026/02/26 対応)
- **対応**: ユーザーの投資ポリシーに基づき、安全性の評価軸を抜本的に見直しました。
  - **投資信託**: 分散が効いているため一律で100点（安全）として評価。
  - **米国個別株**: 情報取得のリスクを考慮し、ウォッチ銘柄として一律10点（リスク）として評価。
  - **国内個別株**: 「ディフェンシブ業種」「時価総額（3,000億/1兆）」「連続増配3年以上」「PBR1.2倍以下」の4項目で各25点の加点方式とし、赤字（ROE < 0）の場合はスコアを半減させる厳格な判定を導入。
  - **分散度補正**: HHI指数が2500を超える集中投資状態の場合、安全性スコア全体に 0.9 倍のペナルティを課すロジックを実装。

### 米国株式の価格・時価総額表示の適正化 (2026/02/26 対応)
- **対応**: 通貨の混在による誤解を防ぐため、ヘッダーと表示内容を整理しました。
  - **現在値**: 米国株はドル建てでの表示であることを明示するため、ヘッダーを「現在値 ($)」または「現在値 (外貨)」に変更。
  - **時価総額**: スクレイパー側で円換算済みであることを明示するため、ヘッダーを「時価総額 (円)」に変更。

### 前日比（比率）の表記統一 [UI改善] (2026/02/26 対応)
- **対応**: 国内株式、米国株式において欠落していた前日比の `%` 表記を追加し、投資信託と合わせて全資産タイプで表記を統一しました。データ形式をバックエンドで正規化し、フロントエンドで一括して単位を付与する設計に刷新しました。

### 決算月表示の不具合修正 (2026/02/25 対応)
- **原因**: Yahoo!ファイナンスの一部ページ（国内株の企業情報など）が Next.js による動的レンダリングに移行したことで、従来の `window.__PRELOADED_STATE__` を用いた JSON 解析ロジックが機能しなくなっていました。
- **対応**: スクレイピングロジックを刷新し、BeautifulSoup による HTML 解析ベースの取得方式に変更しました。また、米国株についても同様の変更に備え、JSON 解析に失敗した場合のフォールバック処理を導入し、取得の安定性を向上させました。

### ポートフォリオ性格診断 (Style Analysis) の実装 [新機能] (2026/02/22 対応)
- **対応**: 保有銘柄の特性から資産全体の性格を自動判定し、視覚化する機能を実装しました。
  - **判定項目**: 「景気敏感/ディフェンシブ」「バリュー/グロース」「大型株/中小型株」の3つの軸で算出. 
  - **診断結果**: 比率が高い要素を組み合わせた簡易的な性格診断メッセージ（一言診断）を表示。
  - **スタックバー表示**: 各スタイルの比率を横棒グラフで直感的に確認できるよう改善。
  - **互換性**: 既存のDNA分析や分散度分析と並列して動作し、フィルタリングにも完全対応。

### ポートフォリオ特性分析 (DNA & Risk) の実装 [新機能] (2026/02/22 対応)
- **対応**: 個別銘柄ではなく「ポートフォリオ全体」を一つのチームとして分析する機能を実装しました。
  - **ポートフォリオのDNA**: 保有額を考慮した加重平均により、全体のPER、PBR、ROE、利回りを算出。市場平均との比較やスタイルの客観視が可能に。
  - **分散度・リスク分析**: HHI指数と上位5銘柄占有率を計算。集中投資によるリスクを視覚化し、「良好」「集中リスクあり」などの診断を表示。

### 配当利回りのリカバリ対応 [改善] (2026/02/20 対応)
- **対応**: Yahoo!ファイナンスから「配当利回り」が取得できない（`N/A`や`--`）場合に、取得済みの「予想年間配当金」と「現在株価」から利回りを自動計算して補完するロジックを実装しました。
  - **メリット**: データ欠損による「総合評価スコア」の低下や「購入注目フラグ」の判定漏れを防止し、より正確な投資判断をサポートします。

### バックエンド定数の外部設定化（リファクタリング） (2026/02/18 対応)
- **対応**: `app.py` にハードコードされていた各種定数（スコア閾値、購入注目フラグの基準、クールダウン時間など）を `highlight_rules.json` に集集約しました。
  - **get_configの導入**: 設定ファイルからの安全な値取得と型変換を行うヘルパー関数を導入。設定ファイルが不足していてもデフォルト値で動作する堅牢な設計に刷新。
  - **柔軟な調整**: アイコン、ラベル名、テクニカル指標の閾値などをコード修正なしで変更可能に。

### 購入注目フラグ (Buy Signal) の実装 (2026/02/16 対応)
- **対応**: 銘柄の「質」と「買い時」を自動判定し、視覚化する機能を実装しました。
  - **判定ロジック**: 「ファンダメンタルズ（業績・割安性）」×「調整（RSI/RCI/フィボナッチ）」×「反転（5日線突破/RSI反転）」の3レイヤーで判定。
  - **ランク分け**: 質の高い銘柄（ファンダ4点以上）を「ダイヤモンド💎」として特別視。
  - **UI表示**: 銘柄名の横に 🔥 などのアイコンを表示し、ホバーで「判定理由」を詳細表示。
  - **フィルタ**: サイドバーから注目銘柄のみを抽出できるフィルタ機能を追加。

### 分析ページの配当分析の不具合修正と名称変更 (2026/02/09 対応)
- **対応**: 分析ページの「保有銘柄一覧」における配当分析の不具合を修正し、指標の名称をより適切なものに変更しました。
  - **不具合修正**: フィルタ（業種や口座など）を適用した際に、配当構成比が「表示されている銘柄のみ」を基準に再計算されてしまう問題を解消しました。常にポートフォリオ全体に対する貢献度が正しく表示されるよう修正しました。
  - **名称変更**: 「配当比率 (%)」という名称が配当利回りと混同されやすかったため、ポートフォリオ内の構成割合であることを示す「**配当構成比 (%)**」へと変更しました。
  - **波及修正**: テーブルヘッダー、CSV出力、および README 内の関連記述をすべて新名称に統一しました。

### テクニカル指標（フィボナッチ・RCI）への説明ツールチップ追加 (2026/02/07 対応)
- **対応**: 専門的なテクニカル指標である「フィボナッチ」と「RCI(26)」の各項目について、その意味や投資判断の基準を即座に確認できるよう改善しました。
  - **ツールチップ実装**: 各ページのテーブルヘッダーに `title` 属性を追加し、マウスホバー時に説明文を表示。
  - **内容**: フィボナッチ（押し目買い圏内の目安）、RCI（底値圏の判断基準）を明記し、初心者でも指標を活用しやすくしました。

### 資産推移・配当積み上げの可視化（グラフ化） (2026/02/05 対応)
- **対応**: 蓄積された履歴データに基づき、資産の成長と配当金の推移を視覚化しました。
  - **資産推移グラフ**: 「総資産額」と「投資元本」を折れ線グラフで表示。含み損益の推移を把握可能に。
  - **配当推移グラフ**: 月ごとの「年間配当予定額」を棒グラフで表示。インカムゲインの成長を可視化。
  - **プライバシー配慮**: 「金額を隠す」機能と連動し、グラフ上の数値もマスクされるよう実装。
  - **キャッシュ対策**: バージョン管理による確実な JS 反映の仕組みを導入。

### 月次資産推移のDB記録基盤の構築 (2026/02/03 対応)
- **対応**: 毎月末時点の資産状況を時系列で記録するためのバックエンド基盤を構築しました。
  - **SQLite導入**: 履歴管理用に `portfolio_history.db` を新規作成し、既存の `portfolio.json` とは独立してデータを管理する安全な設計を採用。
  - **自動スナップショット**: ユーザーが「分析ページ」を開いたタイミングで、その月の最新のポートフォリオ状況（銘柄ごとの評価額、損益、配当など）を自動でDBに保存・更新する仕組みを実装。
  - **API追加**: 履歴データのサマリーを取得するAPIを作成（将来のグラフ化用）。

### 総合評価スコアの表示色分け対応 (2026/02/03 対応)

### 総合評価スコアのトレンド対応と拡張 (2026/02/02 対応)

### 分析ページの配当金分析機能の強化 (2026/02/01 対応)
- **対応**: ポートフォリオにおける配当金の構造を詳細に分析・可視化する機能を追加しました。
  - **サマリー**: 配当が出る銘柄のみを対象とした「配当利回り（現在値ベース）」および「配当利回り（取得値ベース/YOC）」の表示を追加. 無配の資産（再投資型投資信託など）を分母から除外することで、実効的なインカムゲイン効率を把握可能に。
  - **グラフ**: グラフ切り替えに「配当構成（業種別）」を追加。どの業種が配当金の柱になっているかを視覚化。
  - **テーブル**: 保有銘柄一覧に「配当構成比 (%)」列を追加。各銘柄がポートフォリオ全体の配当金にどれだけ貢献しているかを算出。
  - **CSV**: 分析ページのCSV出力に「配当構成比 (%)」を追加。
### 分析ページの証券会社フィルタ・グラフ対応 (2026/01/28 対応)
- **対応**: 分析ページにおいて、登録された「証券会社」情報に基づくフィルタリング機能と、構成比円グラフの表示機能を追加しました。
  - **フィルタ**: 保有銘柄一覧を特定の証券会社で絞り込み、その状態でのサマリー（合計評価額など）を確認できるようになりました。
  - **グラフ**: ポートフォリオ構成グラフに「証券会社別」の切り替えボタンを追加し、資産の分散状況を視覚的に把握できるようにしました。

### 設定ファイル(`portfolio.json`)の構文エラー検知と通知機能 (2026/01/27 対応)
- **対応**: `portfolio.json` を手動編集した際に構文エラー（カンマの重複など）があると、アプリがエラーを出さずにデータが表示されなくなる問題を解消しました。
  - **バックエンド**: JSON パースエラーを明示的にキャッチし、具体的なエラー内容と修正のヒント（JSON Lintへのリンク等）を含むエラーレスポンスを返すように変更。
  - **フロントエンド**: エラーメッセージを HTML 形式でアラート表示できるように拡張し、ユーザーが即座に原因を特定して修正できるように改善。

### 保有銘柄情報の登録項目の拡充 (2026/01/24 対応)
- **対応**: 保有管理情報に「預け先証券会社」と「備考（メモ）」の項目を追加しました。
  - `security_companies.json` で証券会社の選択肢を管理可能に。
  - メインページの保有管理モーダルで入力・更新ができるように UI を拡張。
  - 分析ページのテーブルおよび CSV 出力に「証券会社」と「備考」列を追加。
  - 既存データとの互換性を維持し、未入力項目があっても正常に動作するように堅牢化。

### ポートフォリオ分析のフィルタとサマリーの連動 (2026/01/19 対応)
- **原因**: 分析ページの保有銘柄一覧でフィルタ（テキスト、業種、口座）を適用しても、ページ上部のサマリー情報（総評価額、総損益など）がフィルタ前の全資産の合計値を表示し続けていた。これは、サマリー計算処理が常にフィルタリング前の全データリストを参照していたため。
- **対応**: `static/js/analysis.js` を修正. サマリーを計算する `renderSummary` 関数が、フィルタリング後のデータリストを引数として受け取るように変更。フィルタが適用されるたびに、フィルタ後のデータでサマリーが再計算され、表示が動的に更新されるようにした。

### 分析ページのクールダウン表示の動的化 (2026/01/13 対応)
- **原因**: メインページから分析ページへ短時間で遷移した際、APIのクールダウン中に表示される「データ更新中です...」というメッセージが静的で、残り時間が分からなかった。
- **対応**: `static/js/analysis.js` を修正し、クールダウン中のメッセージが「あと X 秒」のように1秒ごとにカウントダウンする動的な表示になるように変更。これにより、ユーザーはデータが表示されるまでの待ち時間を正確に把握できるようになった。

### ページ遷移時のデータ表示に関するUI/UX改善 (2025/12/31 対応)
- **対応**: ページ遷移時のローディング表示と、クールダウン中の自動再試行ロジックを実装し、ユーザー体験を向上させました。

### 国内株・米国株・投信のデータ取得ロジックの抜本的改善 (2026/04/23 対応)
- **ハイブリッド抽出エンジンの導入**: Yahoo!ファイナンス内の「新方式 (Next.js)」と「旧方式 (JSON)」の技術的混在を自動判別してパースする機能を実装。これにより、全資産タイプの取得安定性が飛躍的に向上しました。
- **国内株の指標精度向上 (ROE/利回り)**: 
    - ROEが負の値（赤字）の際に「自己資本比率」を誤認する問題を解消。
    - 利回りが未定（---）の際に「出来高」を拾ってしまう問題を、オブジェクト境界制約（`{}`内限定探索）の導入により完全に解決。
- **配当履歴の汎用化**: 決算月「3月」固定の制約を廃止。全決算月および複数のデータキー（`amount`, `dps`等）に対応し、連続増配判定を正常化しました。
- **米国株・投信の完全復旧**: これまで `N/A` となっていた米国株の PER、配当利回り、決算月、および投資信託の基準価額の取得を復旧・強化しました。
- **異常データのクレンジング**: 本日の修正前に保存された不正なキャッシュデータを DB から一括削除し、クリーンな再取得環境を整えました。

### 米国株式の時価総額表示の修正 (2025/12/28 対応)
- **原因**: 米国株の時価総額が円換算されずに表示されていた。
- **対応**: 為替レートを用いた円換算処理を追加しました。

